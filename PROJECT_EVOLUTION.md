# 项目演进记录

本文档记录本项目从初始版本到当前状态经历过的主要改进，以及每次改进背后的原因。

## 后续维护规则

以后只要对代码、训练流程、环境建模、奖励函数、可视化、评估脚本或实验默认参数做出有意义的改进，都需要同步更新本文档。

每次更新建议至少写清楚三件事：

- 改了什么；
- 为什么要改；
- 对训练、推理、实验复现或项目结构带来了什么影响。

如果只是训练产生了新的图片、checkpoint、缓存文件，且没有改变代码逻辑或实验结论，可以不用记录。

## 总体演进主线

这个项目最开始是一个能运行的 PPO 卫星追逃 demo。它具备基础环境、PPO 算法、训练入口、测试入口、checkpoint 保存和轨迹可视化。

后续改进的主线逐渐变成：

- 让物理建模更清楚；
- 让 PPO 训练信号更稳定；
- 让实验更容易复现；
- 让训练和推理结果更容易判断；
- 从单 pursuer 训练逐步扩展到更完整的追逃博弈框架。

## 1. 初始版本：建立可运行的 PPO 追逃闭环

最开始的版本创建了项目主体，包括：

- `env.py`：卫星追逃环境；
- `ppo_continuous.py`：连续动作 PPO；
- `replaybuffer.py`：on-policy 经验缓存；
- `main.py`：训练和测试入口；
- `satellite_function.py`：轨道动力学相关函数；
- `plot_function.py`：轨迹绘图；
- `environment.yml` 和 `setup_env.ps1`：运行环境配置；
- 初始 checkpoint 和输出图片。

这个阶段的重点不是追求最优策略，而是先让一个完整强化学习流程跑起来：环境返回状态，策略输出动作，环境推进动力学并给奖励，PPO 更新参数，训练结束后保存模型并可视化轨迹。

改进原因：

没有完整闭环，就无法判断后续任何奖励、动力学或训练参数的调整是否有效。初始版本解决的是“项目能不能跑通”的问题。

## 2. 增加 README：让项目可复现、可交接

随后加入了 `README.md`，说明项目目标、环境准备、训练命令、推理命令、参数含义和常见运行方式。

改进原因：

强化学习项目参数多、运行路径长，只靠代码很难复现实验。README 的作用是降低再次运行、调参、排错和交接成本，让项目从“作者自己能跑”变成“之后还能被看懂和复现”。

## 3. 修正 episode 结束逻辑：区分 terminated 和 truncated

早期环境只返回类似 `done` 的统一结束标记。后来改成：

- `terminated`：捕获成功、燃料耗尽等真实终止；
- `truncated`：达到最大步数造成的时间截断。

PPO 更新中也同步调整：

- 真正终止时不对下一状态做 value bootstrap；
- 时间截断时仍允许使用下一状态价值；
- GAE 递推在 episode 结束处截断，避免跨 episode 传播。

改进原因：

如果把时间上限也当成真实终止，critic 会错误地认为截断后的下一状态没有价值，导致价值估计偏差。这个改动让训练逻辑更符合强化学习理论，也能减少 PPO 更新中的错误学习信号。

## 4. 增加 GPU 自动检测和 CUDA 环境支持

后续加入了：

- `--device auto/cpu/cuda/...` 参数；
- `resolve_device()` 和 `get_device_report()`；
- actor、critic、训练 tensor 自动移动到对应设备；
- `setup_gpu_pytorch.ps1`；
- CUDA 版 PyTorch 的 conda 配置。

改进原因：

PPO 训练需要大量 rollout 和神经网络更新，CPU 实验效率低。自动选择设备可以让同一份代码在 CPU、CUDA 或其他支持的后端上运行，减少手动改代码和环境不一致带来的问题。

## 5. 扩展训练模式和对手策略

项目从早期主要训练 pursuer，扩展到支持：

- `--train-agent pursuer`：只训练追踪方；
- `--train-agent evader`：只训练逃逸方；
- `--train-agent both`：双方同时训练；
- `--opponent-policy fixed`：未训练方使用固定规则；
- `--opponent-policy idle`：未训练方零控制，只随动力学漂移；
- `--opponent-policy learning`：未训练方加载已有 PPO checkpoint。

改进原因：

直接训练双智能体追逃博弈通常不稳定，因为双方策略同时变化，学习目标也在变化。引入 fixed、idle 和 learning 对手后，可以先做课程训练：从简单对手开始，让 pursuer 学会基本接近，再逐步提高对抗难度。

## 6. 重构动力学理解：转向 evader-centered VVLH / CW 相对运动

当前代码逐步把默认场景解释为以 evader 为原点的 VVLH 相对坐标系，并使用 Clohessy-Wiltshire 相对动力学推进 pursuer 的相对状态。

相关改进包括：

- 明确 `EARTH_MU` 和 `GEO_ORBIT_RADIUS` 使用 SI 单位；
- 增加 `propagate_relative()`；
- 环境中将动作解释为脉冲速度增量 delta-v；
- 相对速度更新采用 `pursuer_delta_v - evader_delta_v`；
- evader 在当前相对坐标系中保持原点；
- 新增 `verify_cw_dynamics.py` 验证 CW 动力学和有界初始相对轨道。

改进原因：

追逃问题本质上关心相对位置和相对速度。使用 evader-centered 相对坐标能让状态、距离、动作效果和可视化更容易解释，也避免早期绝对状态推进中可能出现的单位和物理含义混乱。

## 7. 调整任务尺度、动作尺度和奖励函数

项目逐步调整了默认任务参数，例如：

- 初始距离改到更适合默认实验的量级；
- 捕获半径改为更明确的近距离阈值；
- 增加 `--max-relative-distance`，距离过远时终止并惩罚；
- 增加 `--approach-radius`，用于奖励 shaping；
- 增加 `--max-delta-v`，统一控制每步最大脉冲速度增量；
- 增加 `--init-log-std`，降低 Gaussian 策略初始探索噪声；
- 状态缩放改为更贴近当前距离和速度尺度。

奖励也从较粗的启发式叠加，逐步改成同时考虑：

- 距离本身；
- 每一步是否接近目标；
- 相对速度是否朝向目标；
- 动作消耗；
- 速度过大惩罚；
- 远离任务区域惩罚；
- 剩余燃料奖励；
- 捕获成功和逃逸过远的终局信号。

改进原因：

早期任务尺度过大或奖励过稀疏时，PPO 很难从随机探索中学到有效追击行为。动作噪声过大还会导致训练初期大量无意义机动。调任务尺度、动作尺度和奖励，是为了让智能体在训练早期就获得连续、有方向、不过度饱和的学习信号。

## 8. 增强训练和推理可视化

可视化从最初的简单轨迹图，扩展为：

- `outputs/train_reward.png`：训练 reward 曲线；
- `outputs/training_live_dashboard.png`：训练 dashboard；
- `outputs/trajectory.png`：推理代表轨迹；
- `outputs/satellite_game.gif`：带状态面板的追逃动画；
- `outputs/inference_dashboard.png`：多轮推理统计 dashboard。

训练 dashboard 开始记录 reward、距离、捕获情况、loss、学习率、熵、PPO ratio 和最新轨迹。推理阶段也从单轮展示扩展到多 episode 统计。

改进原因：

强化学习结果不能只看一条轨迹。单次成功可能是偶然，单次失败也可能不代表策略整体无效。dashboard 和多轮统计能帮助判断策略是否真的变好，比如捕获率、平均最终距离、平均最近距离是否改善。

## 9. 增加推理统计：从单 episode 到多 episode 评估

当前推理逻辑支持 `--test-episodes`，默认进行多轮确定性推理，并统计：

- 捕获率；
- 平均最终距离；
- 平均最近距离；
- 每轮 reward；
- 每轮步数；
- 代表性 episode 的轨迹和动画。

改进原因：

训练后的策略需要稳定性评估。多 episode 统计比单 episode 更可靠，可以判断 checkpoint 是否真正学到了可复现的追击行为，而不是只在某一次初始条件或随机采样下表现好。

## 10. 增加诊断和参数扫描脚本

当前工作区新增了一些面向实验诊断的脚本：

- `verify_cw_dynamics.py`：验证 CW 相对动力学是否符合预期；
- `verify_parameter_sweep.py`：扫描初始距离、动作幅度等参数；
- `verify_ppo_action_diagnostics.py`：检查 PPO 初始动作分布、动作饱和率、随机策略表现和 checkpoint 策略表现。

改进原因：

这些工具把调参从“凭感觉试”推进到“先诊断问题再改参数”。例如，如果初始 Gaussian 标准差太大，动作可能频繁被 clamp 到边界；如果零动作或规则动作基线都表现异常，就应该先检查动力学和任务尺度，而不是直接归因于 PPO 没学好。

## 11. 双智能体直接训练失败后的稳定化改造

项目早期曾尝试让 pursuer 和 evader 从随机初始化开始同时训练，但没有得到稳定、有效的追逃策略。根本困难在于双方策略同时变化：pursuer 面对的 evader 不断改变，evader 面对的 pursuer 也不断改变，使每一方的状态转移和回报分布都处于非平稳状态。再叠加早期动力学含义、任务尺度、探索噪声和奖励信号不够清晰，PPO 很难判断一次动作究竟是在接近长期目标，还是只是在追逐对手策略的短期变化。

针对这一问题，后续没有继续单纯增加双边训练轮数，而是从训练流程、环境建模和诊断手段三个层面进行了系统改造。

### 11.1 将双边同步学习拆成可控的单边学习任务

训练入口从只支持直接对抗，扩展为 `--train-agent pursuer`、`evader` 和 `both` 三种模式，并为未训练的一方增加三类对手：

- `idle`：对手不主动机动，只按 CW 动力学自然漂移；
- `fixed`：对手使用固定规则，行为稳定且可解释；
- `learning`：对手加载已有 checkpoint，以冻结或相对稳定的学习策略参与对抗。

这样可以先训练 pursuer 对付 idle 对手，使其学会最基础的接近和捕获；再换成 fixed 对手提高任务难度；最后使用已有策略作为 learning 对手，或在双方都具有基本能力后再进入 `both` 模式。这个流程把原来一步到位的双智能体训练改成由易到难的课程训练，降低了双方从随机策略开始相互制造噪声的难度。

### 11.2 修正动力学与 episode 边界，减少错误学习信号

环境统一为 evader-centered VVLH 坐标系，并使用 CW 方程推进相对运动。动作被明确解释为每一步的脉冲速度增量，双方作用于相对状态的控制量统一为 `pursuer_delta_v - evader_delta_v`。同时补充 CW 有界初始相对速度，避免 idle 对手场景因为不合理初始条件产生过强的自然漂移。

episode 结束也被拆分为 `terminated` 和 `truncated`：捕获、燃料耗尽或越界属于真实终止，达到最大步数属于时间截断。PPO 在时间截断时仍对下一状态做 value bootstrap，但在真实终止时停止 bootstrap，并在 GAE 递推中阻断跨 episode 传播。这些修正避免 critic 把时间上限误学成零价值，也防止优势估计跨越不同对局相互污染。

### 11.3 缩小任务与动作尺度，控制训练初期探索噪声

为让随机初始化的策略更容易获得有效反馈，项目重新调整了初始距离、捕获半径、最大相对距离、单步动力学时间和 `--max-delta-v`，并对状态中的位置、速度分量分别缩放。Gaussian actor 增加 `--init-log-std`，用较小的初始标准差降低无意义的大幅机动和动作饱和。

这些改动使 PPO 输入、动作和实际轨道变化处于更匹配的数值范围内。训练初期的探索不再频繁把动作推到边界，智能体也更容易从短距离、低难度任务中先学到方向正确的行为。

### 11.4 重构奖励，使追踪方能够持续获得有方向的反馈

奖励从稀疏的捕获结果和较粗的启发式叠加，改为同时利用：

- 当前距离及进入接近区的程度；
- 相邻两步的距离变化；
- 相对速度是否指向目标；
- 捕获成功的终局奖励；
- 逃出最大任务范围的惩罚；
- 动作消耗、速度过大和燃料使用惩罚；
- 剩余燃料奖励；
- 对不必要的轨道面法向机动的惩罚。

其中，捕获边界附近的 shaping 和可配置的 `--capture-reward` 强化了“跨过捕获阈值”这一最终目标；`--capture-margin-scale` 控制捕获区外连续奖励的作用范围；`--normal-delta-v-penalty-coef` 抑制对当前轨道面法向的低收益机动。相比只在成功时给出一次奖励，这套设计能让 pursuer 在大部分训练步中都获得关于接近方向、控制效率和最终捕获目标的一致反馈。

### 11.5 增加基线、诊断和多轮评估，区分环境问题与算法问题

新增的 `verify_cw_dynamics.py`、`verify_parameter_sweep.py` 和 `verify_ppo_action_diagnostics.py` 分别用于验证 CW 动力学、扫描任务与动作参数、检查初始动作分布和动作饱和率。idle、fixed 和零动作策略也构成了不同难度的可解释基线。

训练过程增加 reward、episode 步数、距离、捕获率、loss、熵和 PPO ratio 等 dashboard 指标；推理则改为多 episode 确定性评估，统计捕获率、平均最终距离和平均最近距离。这样可以先确认动力学和基线是否合理，再判断 PPO 是否真正学到稳定策略，避免把环境尺度、奖励或动作分布的问题笼统归因于“双智能体学不会”。

### 11.6 改造后的训练思路与影响

这一系列改进形成了新的训练路线：先验证动力学和零动作基线，再让 pursuer 在 idle 对手下学习基本捕获，随后逐步切换到 fixed、learning 对手，最后才尝试双边联合训练或交替训练。

其核心影响是把一个高度非平稳、反馈稀疏且难以定位问题的双智能体任务，拆成若干可训练、可验证、可逐步加难的阶段。idle 对手场景下已经能够训练出可用的 pursuer checkpoint，说明单边追踪学习闭环可以成立；这为后续训练 evader、构建策略池、交替冻结双方策略以及恢复双智能体对抗训练提供了更可靠的起点。需要注意的是，这些改进解决的是训练基础设施、信号质量和课程难度问题，并不意味着从随机初始化直接同步训练双方已经天然稳定；双边对抗仍应建立在双方已有基础能力和可重复评估的前提上。

## 当前状态总结

截至当前，项目已经从一个基础 PPO 追逃 demo，演进为一个更完整的卫星相对运动强化学习实验框架。

当前已经具备：

- PPO 连续动作训练；
- pursuer、evader 和双边训练模式；
- fixed、idle、learning 三类对手策略；
- evader-centered CW 相对动力学；
- 状态缩放、动作限幅和燃料约束；
- 训练 dashboard；
- 多轮确定性推理评估；
- 推理 dashboard、轨迹图和 GIF；
- GPU 自动检测；
- 动力学验证、参数扫描和动作诊断脚本。
- 面向双智能体非平稳问题的分阶段课程训练路线。

项目后续最重要的方向，是继续提高训练稳定性和实验可信度。每次代码层面的改进，都应同步记录在本文档中，形成清晰的项目演进脉络。
