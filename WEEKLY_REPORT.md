# 卫星追逃强化学习项目周报

## 一、已完成工作

本周主要针对双智能体从随机策略开始直接训练不稳定的问题，对训练流程和环境进行了改进：

- 将直接双边对抗拆分为分阶段训练，支持分别训练 pursuer、evader 或双方；
- 增加 idle、fixed 和 learning 三类对手，为后续课程训练提供不同难度的训练环境；
- 统一采用 evader-centered VVLH 坐标系和 CW 相对动力学，明确双方动作对相对状态的影响；
- 区分 `terminated` 和 `truncated`，修正 PPO 的 value bootstrap 和 GAE 计算；
- 调整初始距离、捕获半径、动作幅度、状态缩放及策略初始探索噪声；
- 重构奖励函数，增加距离变化、接近方向、捕获结果、燃料消耗和轨道面法向机动等反馈；
- 增加动力学验证、参数扫描、动作分布诊断、训练 dashboard 和多轮推理评估；
- 已在 idle 对手场景下训练出可用的 pursuer checkpoint，验证了单边追踪训练闭环。

## 二、未完成工作及问题

- pursuer 和 evader 从随机初始化开始同步训练仍不稳定，双方策略同时变化会导致训练环境非平稳；
- 当前主要验证了 pursuer 对 idle 对手的训练效果，evader 以及更复杂对手下的训练尚未充分完成；
- 从 idle 对手切换到 fixed 或 learning 对手时，策略能否稳定迁移仍需实验验证；
- 奖励函数包含较多 shaping 项，各项权重对最终行为的影响还缺少系统消融实验；
- 当前确定性推理使用固定初始条件，多轮结果可能重复，尚不能充分反映策略的泛化能力；
- 双智能体训练所需的对手更新方式、冻结周期和 checkpoint 策略池机制尚未确定。

## 三、未来两周计划

### 第一周：完善单边训练和评估基线

- 扩展 pursuer 在不同初始距离、初始速度和捕获半径下的 idle 对手实验；
- 训练并评估 pursuer 对 fixed 对手的策略，验证课程难度提升后的迁移效果；
- 开展奖励函数消融实验，重点检查距离 shaping、捕获奖励和动作惩罚的作用；
- 为推理阶段增加随机化初始条件，统计捕获率、最近距离、燃料消耗和任务完成时间；
- 整理各实验的配置、checkpoint 和评估结果，建立可重复的对照基线。

### 第二周：逐步恢复双智能体对抗训练

- 训练基础 evader 策略，并分别使用 fixed pursuer 和已有 pursuer checkpoint 进行验证；
- 尝试交替冻结训练：固定 evader 更新 pursuer，再固定 pursuer 更新 evader；
- 建立简单的历史 checkpoint 对手池，降低只针对最新对手过拟合的风险；
- 对比同步训练、交替训练和课程训练的稳定性及最终追逃表现；
- 根据实验结果确定下一阶段的双智能体训练方案和主要参数。

